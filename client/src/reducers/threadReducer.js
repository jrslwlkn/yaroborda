const initialState = {
    url: '',
    opPost: {},
    posts: [],
    threadIsLoading: false,
};

export default (state = initialState, action) => {
    switch (action.type) {
    default:
        return state;
    }
};
